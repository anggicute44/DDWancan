package id.app.ddwancan.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors
val PrimaryBlue = Color(0xFF2678FF) // Biru cerah untuk light mode
val LightBlue = Color(0xFFEFF3FF)   // Background terang atau aksen
val TextDark = Color(0xFF1C1C1C)    // Teks utama untuk light mode

// Dark Theme Colors
val DarkPrimary = Color(0xFF3F8CFF) // Biru yang sedikit lebih cerah untuk kontras di dark mode
val DarkBackground = Color(0xFF121212) // Background utama dark mode (hitam pekat)
val DarkSurface = Color(0xFF1E1E1E)   // Warna permukaan untuk card, dialog, dll.
val TextLight = Color(0xFFE0E0E0)     // Teks utama untuk dark mode
val TextSlightlyDim = Color(0xFFB0B0B0) // Teks sekunder atau ikon di dark mode

// Neutral Colors (can be used in both themes if needed)
val RedError = Color(0xFFD32F2F)
val YellowWarning = Color(0xFFFFA000)
